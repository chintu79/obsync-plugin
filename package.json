{
  "name": "obsync-plugin",
  "version": "1.0.1",
  "description": "Obsync — free P2P sync for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "obsidian",
    "sync",
    "p2p",
    "offline-first"
  ],
  "license": "MIT OR Apache-2.0",
  "dependencies": {
    "@noble/ciphers": "^1.0.0",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.28.1",
    "obsidian": "^1.5.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
